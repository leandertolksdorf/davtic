import {DavticConfig} from "./lib/davticConfig.type";

const davticConfig: DavticConfig = {
  caldav: {
    server: "mycaldavserver.com",
    password: "helloworld"
  }
}
export default davticConfig;
